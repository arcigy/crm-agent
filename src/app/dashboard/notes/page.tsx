import NotesTool from '@/tools/notes/page';

export const metadata = {
    title: 'Notes | CRM',
    description: 'Rýchle poznámky a myšlienky',
};

export default function NotesPage() {
    return <NotesTool />;
}
